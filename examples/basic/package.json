{
  "name": "basic",
  "version": "0.0.0",
  "description": "Basic example",
  "scripts": {
    "dev": "react-power dev",
    "build": "react-power build",
    "clean": "react-power clean"
  },
  "dependencies": {
    "react-power": "^0.2.0"
  }
}
